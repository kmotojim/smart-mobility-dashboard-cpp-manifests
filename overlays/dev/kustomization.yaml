apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: smart-mobility-dev-cpp

commonLabels:
  environment: development

resources:
  - ../../base
  - namespace.yaml

patches:
  - target:
      kind: Namespace
      name: smart-mobility-cpp
    patch: |
      $patch: delete
      apiVersion: v1
      kind: Namespace
      metadata:
        name: smart-mobility-cpp

images:
  - name: smart-mobility-backend-cpp
    newName: image-registry.openshift-image-registry.svc:5000/smart-mobility-dev-cpp/backend-cpp
    newTag: dev

replicas:
  - name: smart-mobility-backend-cpp
    count: 1
